# Zinx Exhibit Kit · 展示系统规范 0.4

日期：2026-09-22。面向可持续更新的个人产品册，也作为后续可售模板的设计基础。本轮为本地实现，销售包装、完整许可证清单和上线仍有独立验收门槛。

## 核心体验

访问者先看到可玩的老虎机，或通过作品链接直达内容。作者编排一条清楚的参观顺序，所有作品始终可读、可打开。YY 提供陪伴和局部游戏反馈；重点信息由真实产品承担。

声音默认开启，首个点击/按键后播放。滚动保持浏览器原生行为。看过标记表示本次展厅访问中打开过详情，不代表阅读完成；不设付费、解锁或通关门槛。

## 统一造型：圆角、倒角与像素玻璃

圆角决定友好、饱满的轮廓；倒角让边缘显出厚度与受光方向。这两项是 Zinx 的核心辨识度。代码感体现在清晰对齐、短编号、语义状态和像素标记中。赌场暗场贯穿入口与金币展台，金色收藏币承载真实作品，四色保留在品牌符号与分类中。

| 层级 | 代码位置 / 参数 | 使用规则 |
| --- | --- | --- |
| 设计变量 | `src/design/tokens.css` | 颜色、圆角、边界、空间、时长和缓动的共享入口 |
| 外壳 | `--zx-radius-shell: 24px` | 展框、详情弹窗、作者面板 |
| 内面板 | `--zx-radius-panel: 18px` | 6px 内嵌后与外壳同心；截图完整显示 |
| 控件 | `--zx-radius-control: 14px` | 按钮、媒体选择、开场入口 |
| 标题栏 | `PanelBar.tsx` | 左侧短编号、中间可读标题、右侧状态或真实动作；不放假的关闭/最小化按钮 |
| 玻璃倒角 | `--zx-glass-rim` / `--zx-contact-shadow` | 上侧白高光、下侧暗倒角、双层同心细缘、2–8px 短接触阴影；文字和截图下方有实色托底 |
| 主色 | 蓝 `#2477ff`、红 `#e7372f`、黄 `#ffd33d`、绿 `#16b35c` | 像素标记、边缘透色、拉杆与状态；颜色同时配文字 |
| 空间 | 4 / 8 / 12 / 16 / 24 / 32px | 控件紧凑、媒体舒展、展区有停顿 |
| 字体 | 现有现代粗黑体 + 等宽短编号 | 正文保持中文可读；不使用大段像素字体或代码替代说明 |

环境底色为 `--zx-salon: #160a13`，金币展台 `--zx-felt: #2b1521fc`，香槟选中控件 `--zx-champagne: #f6d8b1`。币面采用金属金与深褐文字，详情阅读区沿用浅底深字。四色用于品牌符号和分类，真实媒体保持原色。光附着在有厚度的币缘和实体表面；新组件接入时映射同一套材质。

## 组件与行为契约

- **入口**：`/` 或 `#top` 总是展示老虎机；`#hub` 直达展厅；`#case-ID` 直达可见作品。开场不依赖历史解锁缓存。展厅顶部常驻“老虎机开场”。
- **独立作品金币**：最大直径 280px，中心使用真实产品标识的金属浮雕与彩色珐琅。哑光拉丝底、7px 币边、同心铸圈、齿纹与金属反光；正反面和币边共用 coin-turner。悬浮抬升 16px、X/Y 最大 8°/10° 倾斜；减少动效关闭旋转过渡和位移。编号与分类沿下缘铸刻，已看标记置于币下。
- **操作文案**：拉杆旁依状态显示“下拉启动 / 再拉一次 / 最后一拉”。机身仅陈列轮次灯、ZINX 灯箱及真实作品图标贴纸；键盘空格和无障碍跳过链接保留。YY 动效跟随系统减少动态效果设置。展台“重新散币”，底部“一键翻开 / 全部收回”。
- **金币铸纹**：CoinRim 提供双层同心边圈、齿纹与四方菱形；背面为 ZINX 微型印记、像素 Z 字章及编号。币面装饰不接收输入，名称与状态置于币下常驻。
- **重新散币与翻面**：重新散币先收回正面，再将当前分类聚拢、散开。悬停/键盘焦点临时翻面，触摸首次翻面、再次打开；一键翻开固定全部正面，一键收回恢复背面。翻面与已查看详情分别记录。跳过、重玩、卸载清理时间线与声音。
- **详情**：原生 dialog，Esc/关闭返回展框，保留焦点；图片、视频、SVG 动作、动图和本地 demo 使用同一媒体契约。
- **顺序浏览**：上一件/下一件只取当前可见清单，隐藏项不参与导航，首尾禁用对应按钮。
- **媒体**：金币加载真实标识，`emblem` 缺失或加载失败时使用名称缩写。截图、视频与动图保留在详情；详情视频和 demo 点击加载、关闭卸载。外部应用链接明确打开新页，本地指南使用沙箱。
- **YY**：现行 SVG、无脚、现有衣物与动作；点击加演、换装、暂停可见；手机有独立安全区，详情弹窗覆盖时背景不可操作。
- **浏览进度**：看过数与像素格来自真实打开记录，按稳定 ID 去重；本次展厅卸载后重置，不进行用户追踪。
- **历史目录**：旧 `?view=library` 访问统一进入当前系统，保留 `#about` 和案例深链；历史页面源码留存但不挂载，避免从旧入口再次展示未选作品。

## 动效时序

| 动作 | 时长 / 方法 | 静态与减少动效 |
| --- | --- | --- |
| 机器开场 | 三次操作各转一轮；第三轮后 2.4 秒冒烟、爆开、抛币落桌 | 三轮短状态推进，第三轮直接进入展台 |
| 控件响应 | 160ms | 保留选中、边框与文字状态 |
| 展框反馈 | 280ms ease-out | 停止位移 |
| 金币入桌 | 爆开后金币沿抛物线落桌；作品金币背面朝上入场 | 直接显示背面，可一键翻开 |
| 详情展开 | 480ms；原展框到详情的位置关联 | 即时出现 |
| 详情关闭 | 300ms；回到原展框 | 即时关闭 |
| YY 循环 | 现行源生舞句 | 页面隐藏、减少动效或暂停时停止 |

动效采用现有 GSAP 和 React Three Fiber 能力；当前没有新增动画引擎。动态正文、强制滚动劫持、遮挡产品的持续粒子、每张卡自动播视频均不纳入默认模式。

声音采用原创 Web Audio 合成：低频节拍、带通噪声齿音、升调与停轮三音、入场和弦。滚轮时长与声谱共用数据；主音量 0.48、压缩器控制动态。静音立即停止已排程声源，页面隐藏、跳过、重玩与卸载清理声音，恢复音量时不会补播旧音。声音偏好独立于机械时间线。

## 内容模型

`portfolio.ts`（现有事实）或 `extra-exhibits.ts`（新增作品）→ `exhibitCatalog`（统一媒体与 preview）→ `exhibitionPlacements`（明确选入）→ ProjectCard / 详情 / 导航 / 进度。

preview.mediaId 选定预览媒体；preview.caption 为一句具体用途。media 数组决定详情顺序；无效预览 ID、重复 ID、错误分组在内容测试时失败。花色代表类型，序号代表顺序；不限制作品数量，不设抽中或通关门槛。新增步骤见 ADDING_WORKS.md。

未明确设 `visible: true` 的新作品保持候选；没有媒体的条目保持隐藏。可见条目共用一份有序数组。新增内容无需修改相机、老虎机滚轮或页面数量常量。

## 外部资源与可售边界

详见 `RESOURCE_REFERENCES.md`。模板可以围绕自己的策展模型、组件、视觉规范和交互体验形成产品；实际销售前单独检查依赖许可、素材再分发权和示例内容授权。

建议未来分别包装：设计规范与组件、可配置作品册模板、可选老虎机开场模块。YY 是 Zinx 的独立 IP，默认属于个人示例层；售卖通用模板时提供中性示例、明确品牌/IP 的使用范围。当前接口支持媒体与内容替换，尚无可视化管理后台或无代码动画编辑器。

## 每次修改的验收清单

- 首页可启动机器；三次输入各转一轮，第三轮冒烟爆开金币并自动入场；直达链接和跳过、重玩可用。
- 只显示本轮选择的作品；新加目录条目不会自动进入公开展厅。
- 1366×768、1440×900 和 390×844 下无横向溢出，标题、CTA、媒体完整；短屏可滚动查看详情。
- 鼠标、触摸、键盘均可打开/关闭、前后浏览；关闭后焦点可见；快速重复输入不打乱开场。
- 视频、demo、四种 YY 动作实际可用；对关闭、隐藏标签页和减少动效做回归。
- 共享 token / PanelBar 保持一致；大面板、内框、按钮均有圆角和有方向的倒角层次；正文对比不低于 4.5:1，触控区域至少 44px。
- 构建、lint、测试、资产白名单与源生 YY 哈希通过；本地验证与线上发布分开记录。
