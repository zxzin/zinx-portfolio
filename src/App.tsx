import PortalEntrance from "./components/PortalEntrance";

/** Keep route and content updates inside the React refresh boundary. */
export default function App() {
  // Historical library URLs use the same curated selection and current visual system.
  return <PortalEntrance />;
}
